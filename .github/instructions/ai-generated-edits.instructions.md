---
applyTo: "*"
---

Add a comment to the top of the file,
replace the `$MODEL` placeholder with the model that was used,
then add an empty line.

Do not make any other changes.

Use the following template and format:

<template>
Contains AI-generated code
MODEL USED: $MODEL
</template>
