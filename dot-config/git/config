[core]
    eol = lf
    autocrlf = false
    whitespace = "trailing-space,space-before-tab"
    excludesfile = ~/.config/git/ignore

[user]
    email = "serverhorror@users.noreply.github.com"
    name = "serverhorror"

[alias]
    br = branch
    ci = commit
    civ = commit -v
    p = pull
    st = status
    sw = switch

    # summary log
    slog = shortlog --summary --group=format:"%aE" --numbered

    # convenience commands
    branches = for-each-ref --sort=-committerdate --format=\"%(color:blue)%(authordate:relative)\t%(color:red)%(authorname)\t%(color:white)%(color:bold)%(refname:short)\" refs/remotes
    force-push = push --force-with-lease
    reset-upstream = reset --hard '@{u}'
    root = rev-parse --show-toplevel
    cd-root = "!f() { cd $(git root) ; }; f"
    alias = "!f() { git config --get-regexp "^alias.${1}" ; }; f"

[merge]
    ff = only

[fetch]
    prune = false

[push]
    default = current

[rebase]
    autosquash = true

[rerere]
    enabled = true

[credential]
    credentialStore = "cache"
    cacheOptions = "--timeout 86399"
    helper = "cache --timeout 86399"
    helper = "manager"
    useHttpPath = "true"

[credential "https://dev.azure.com"]
    useHttpPath = "true"

[credential "https://huggingface.co"]
    provider = "generic"

[credential "https://gist.github.com"]
    helper = "cache --timeout 86399"
    helper = "manager"

[credential "https://github.com"]
    helper = "cache --timeout 86399"
    helper = "manager --no-ui"
    provider = "github"

[includeIf "hasconfig:remote.*.url:https://bitbucket.biscrum.com/**"]
    path = "~/.config/git/boehringer-ingelheim.inc"

[includeIf "hasconfig:remote.*.url:https://config-repo.werum.com/**"]
    path = "~/.config/git/boehringer-ingelheim.inc"

[includeIf "hasconfig:remote.*.url:https://github.com/opendevstack/**"]
    path = "~/.config/git/boehringer-ingelheim.inc"
[credential "https://bitbucket.biscrum.com/scm/arcf/arcf-docroot.git"]
	provider = bitbucket
[credential "https://bitbucket.biscrum.com/scm/arcf/arcf-contribution-guide.git"]
	provider = bitbucket
