# This is an example Hyprland config file.
# Refer to the wiki for more information.
# https://wiki.hyprland.org/Configuring/

# Please note not all available settings / options are set here.
# For a full list, see the wiki

# You can split this configuration into multiple files
# Create your files separately and then link them to this file like this:
# source = ~/.config/hypr/myColors.conf


################
### MONITORS ###
################

$mainMonitor = HDMI-A-1
$right = HDMI-A-2

# binds
$mainMod = SUPER # Sets "Windows" key as main modifier

# See https://wiki.hyprland.org/Configuring/Monitors/
# monitor=,preferred,auto,auto
#monitor = $mainMonitor, preferred, 0x0, 1
#monitor = $right, preferred, 3840x-860, 1, transform, 3
monitor = $mainMonitor, preferred, 0x0, 1.5
monitor = $right, preferred, 2560x-1185, 1, transform, 3


###################
### MY PROGRAMS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/

# Set programs that you use
$terminal = kitty
$fileManager = nautilus
$menu = rofi -show drun


#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

# background images
# exec-once = swaybg --output $mainMonitor --image ~/Pictures/pexels-steve-1269968.jpg
# exec-once = swaybg --output $right --image ~/Pictures/pexels-steve-1269968_vertical.png

# bar and applets
exec-once = dunst
exec-once = waybar
exec-once = hyperidle
exec-once = hyprpaper
exec-once = nm-applet
exec-once = emacs --daemon
# exec-once = ~/.config/hypr/scripts/screenkey.sh





#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hyprland.org/Configuring/Environment-variables/

env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24

# elctron apps like VS Code
env = ELECTRON_OZONE_PLATFORM_HINT=auto


###################
### PERMISSIONS ###
###################

# See https://wiki.hyprland.org/Configuring/Permissions/

# ecosystem {
#   enforce_permissions = 1
# }

# permission = /usr/(bin|local/bin)/grim, screencopy, allow
# permission = /usr/(lib|libexec|lib64)/xdg-desktop-portal-hyprland, screencopy, allow
# permission = /usr/(bin|local/bin)/hyprpm, plugin, allow


#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general
general {
    gaps_in = 5
    gaps_out = 10

    border_size = 2

    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    # col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    # col.active_border = rgba(33ccffee)
    col.active_border = rgba(51,204,255,0.90)
    col.inactive_border = rgba(89,89,89,0.0)

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = false

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    layout = master
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 0
    # rounding_power = 2

    # Change transparency of focused and unfocused windows
    active_opacity = 0.99
    inactive_opacity = 0.92
    dim_inactive = false

    shadow {
        enabled = false
        range = 4
        render_power = 3
        color = rgba(124, 135, 153, 1)
    }

    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 3
        passes = 3

        # vibrancy = 0
        # vibrancy = 0.1696
        # vibrancy = 0.32
        # vibrancy = 1

        vibrancy_darkness = 0
        # vibrancy_darkness = 1
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    # enabled = yes, please :)
    enabled = true

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1

    animation = global, 1, 1.0, default
    animation = border, 1, 1.0, easeOutQuint
    animation = windows, 1, 1.0, easeOutQuint
    animation = windowsIn, 1, 1.0, easeOutQuint, popin 87%
    animation = windowsOut, 1, 1.0, linear, popin 87%
    animation = fadeIn, 1, 1.0, almostLinear
    animation = fadeOut, 1, 1.0, almostLinear
    animation = fade, 1, 1.0, quick
    animation = layers, 1, 1.0, easeOutQuint
    animation = layersIn, 1, 1.0, easeOutQuint, fade
    animation = layersOut, 1, 1.0, linear, fade
    animation = fadeLayersIn, 1, 1.0, almostLinear
    animation = fadeLayersOut, 1, 1.0, almostLinear
    # animation = workspaces, 1, 1.0, almostLinear, fade
    # animation = workspacesIn, 1, 1.0, almostLinear, fade
    # animation = workspacesOut, 1, 1.0, almostLinear, fade
}

# Ref https://wiki.hyprland.org/Configuring/Workspace-Rules/
# "Smart gaps" / "No gaps when only"
# uncomment all if you wish to use that.
# workspace = w[tv1], gapsout:0, gapsin:0
# workspace = f[1], gapsout:0, gapsin:0
# windowrule = bordersize 0, floating:0, onworkspace:w[tv1]
# windowrule = rounding 0, floating:0, onworkspace:w[tv1]
# windowrule = bordersize 0, floating:0, onworkspace:f[1]
# windowrule = rounding 0, floating:0, onworkspace:f[1]

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
master {
    allow_small_split = false
    new_status = slave
    new_on_top = true
    orientation = right
    mfact = 0.66
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc {
    force_default_wallpaper = -1 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = false # If true disables the random hyprland logo / anime girl background. :(
    new_window_takes_over_fullscreen = 1 # 0 - behind, 1 - takes over, 2 - unfullscreen/unmaxize
}


#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options = caps:escape
    kb_rules =
    follow_mouse = 1
    mouse_refocus = true

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = false
    }
    tablet {
        output = $mainMonitor
    }
}

# # https://wiki.hyprland.org/Configuring/Variables/#gestures
# gestures {
#     workspace_swipe = false
# }

group {

    col.border_active = rgba(33ccffee)
    col.border_inactive = rgba(595959aa)
    insert_after_current = true

    groupbar {
        enabled = true
        stacked = false
        height = 30
        font_size = 24
        font_weight_active = bold
        font_weight_inactive = normal
        text_color = rgba(0, 0, 0, 1.0)
        render_titles = true

        gradients = true
        col.active = rgba(33ccffee)
        col.inactive = rgba(595959aa)

        keep_upper_gap = false
        gaps_in = 0
        gaps_out = 1
    #     enabled = true
    #     position = top
    #     height = 30
    #     padding = 0
    #     color = rgba(0, 0, 0, 1.0)
    #     bordercolor = rgba(255, 255, 255, 1.0)
    #     borderwidth = 2
    }
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}


###################
### KEYBINDINGS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/


# Screenshot
bind = ,print, exec, grimshot copy area

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, return, exec, $terminal
bind = $mainMod, C, killactive,
bind = $mainMod SHIFT, Q, forcekillactive,
bind = $mainMod, Q, killactive,
bind = $mainMod SHIFT, C, forcekillactive,
bind = $mainMod CTRL, L, exec, hyprlock
bind = $mainMod, E, exec, $fileManager

# fullscreen & floating
bind = $mainMod, F, fullscreenstate, 1, 3
bind = $mainMod SHIFT, F, fullscreenstate, 2, 3
# bind = , F11, fullscreenstate, 1, 3
bind = $mainMod SHIFT, V, exec, ~/.config/hypr/scripts/togglefloating.sh
# /fullscreen & floating

# menu
bind = $mainMod, O, exec, $menu
bind = $mainMod, space, exec, $menu
# bind = CTRL, escape, exec, $menu
# bind = CTRL, space, exec, $menu
# /menu




# bind = $mainMod, P, pseudo, # dwindle
# bind = $mainMod + CTRL, J, togglesplit, # dwindle

# Move focus with mainMod + vim movements
bind = $mainMod, h, movefocus, l
bind = $mainMod, l, movefocus, r
bind = $mainMod, k, movefocus, u
bind = $mainMod, j, movefocus, d
# Move focus with mainMod + SHIFT + vim movements
bind = $mainMod SHIFT, h, movewindow, l
bind = $mainMod SHIFT, l, movewindow, r
bind = $mainMod SHIFT, k, movewindow, u
bind = $mainMod SHIFT, j, movewindow, d
# Move focus with mainMod + SHIFT + vim movements

bind = $mainMod, tab, cyclenext

# # groups
# bind = $mainMod ALT, h, movewindoworgroup, l
# bind = $mainMod ALT, l, movewindoworgroup, r
# bind = $mainMod ALT, k, movewindoworgroup, u
# bind = $mainMod ALT, j, movewindoworgroup, d
# # bind = $mainMod, tab, changegroupactive,
# bind = $mainMod shift, tab, changegroupactive,
# # /groups


# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# # # Yeah ... this is kind of stupid ...
# # You can use a single mod with multiple keys.
# binds = Control_L, A&Z, exec, kitty

# go to the next/previous workspace
bind = $mainMod, escape, workspace, previous
# bind = $mainMod SHIFT, escape, focuscurrentorlast
bind = $mainMod SHIFT, escape, focusurgentorlast

# Switch to the next/previous workspace
bind = $mainMod, N, workspace, e+1
bind = $mainMod, P, workspace, e-1

# Move active window to the next/previous workspace
bind = $mainMod SHIFT, S, swapactiveworkspaces, HDMI-A-1 HDMI-A-2


# submap:Test
bind = $mainMod , T, submap, Test
submap = Test

# bind = $mainMod, i, exec, [workspace 9 silent; fullscreenstate 1 3] firefox -P Kiosk --private-window https://excalidraw.com
bind = , i, exec, [workspace 8; fullscreenstate 1 3] kitty && hyprctl dispatch submap reset
# temporary!
bind = , w, exec, killall -SIGUSR2 waybar

# use reset to go back to the global submap
bind = $mainMod, T, submap, reset
bind = , escape, submap, reset

# will reset the submap, which will return to the global submap
submap = reset
# /submap:Test

# submap:MoveWorkspace
bind = $mainMod , M, submap, MoveWorkspace
submap = MoveWorkspace
# sets repeatable binds for resizing the active window
binde = , right, movecurrentworkspacetomonitor, r
binde = , l, movecurrentworkspacetomonitor, r
binde = , left, movecurrentworkspacetomonitor, l
binde = , h, movecurrentworkspacetomonitor, l
bind = ,c , centerwindow
# use reset to go back to the global submap
bind = $mainMod, M, submap, reset
bind = , escape, submap, reset
# will reset the submap, which will return to the global submap
submap = reset
# /submap:MoveWorkspace

# # Example special workspace (scratchpad)
# bind = $mainMod, S, togglespecialworkspace, magic
# bind = $mainMod SHIFT, S, movetoworkspace, special:magic


# submap:special:apps
bind = $mainMod, S, submap, SpecialWS
submap = SpecialWS
# special workspace (apps)
bind = , a, togglespecialworkspace, apps
bind = SHIFT, a, movetoworkspace, special:apps
bind = , s, togglespecialworkspace, social
bind = SHIFT, s, movetoworkspace, special:social
# bind = $mainMod SHIFT, S, movetoworkspace, special:apps
# use reset to go back to the global submap
bind = $mainMod, S, submap, reset
bind = , escape, submap, reset
# will reset the submap, which will return to the global submap
submap = reset
# submap:special:apps

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl s 10%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl s 10%-

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

# # autosettings
# exec-once = ~/.config/hypr/scripts/workspace-names.sh
# # rename workspaces
# bind = $mainMod, F1, exec, ~/.config/hypr/scripts/workspace-names.sh

##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules
# workspace = m[$mainMonitor], layoutopt:orientation:right

workspace = 1, monitor:$mainMonitor, default:true, persistent:true
exec-once = [workspace 1 silent] code --new-window && $terminal

workspace = 2, monitor:$mainMonitor
exec-once = [workspace 2 silent] firefox && $terminal

workspace = 3, monitor:$mainMonitor
exec-once = [workspace 3 silent] microsoft-edge && terminal

workspace = 4, monitor:$mainMonitor
exec-once = [workspace 4 silent; fullscreenstate 1 3] gimp --no-splash # Gimp
exec-once = [workspace 4 silent; fullscreenstate 1 3] firefox -P Kiosk --kiosk --private-window https://excalidraw.com

###

workspace = m[$right], layoutopt:orientation:bottom

workspace = 10, monitor:$right, default:true, persistent:true

# Autostart Special Workspace Apps
workspace = special:apps, monitor:$mainMonitor
exec-once = [workspace special:social silent] /home/serverhorror/bin/spotify  # utils
exec-once = [workspace special:social silent] /home/serverhorror/bin/slack  # utils
exec-once = [workspace special:social silent] /home/serverhorror/bin/discord  # utils

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# Example windowrule
# windowrule = float,class:^(kitty)$,title:^(kitty)$

## Borders
windowrule = bordercolor rgb(FF0000) rgb(880808), fullscreen:1 # set bordercolor to red if window is fullscreen
windowrule = bordersize 5, fullscreen:1 # set bordercolor to red if window is fullscreen

windowrule = bordercolor rgb(00FF00), fullscreenstate:* 1 # set bordercolor to green if window's client fullscreen state is 1(maximize) (internal state can be anything)
windowrule = bordersize 5, fullscreenstate:* 1 # set bordercolor to green if window's client fullscreen state is 1(maximize) (internal state can be anything)

# Important windows stay!
windowrule = stayfocused, class:(pinentry-)(.*) # fix pinentry losing focus


# Ignore maximize requests from apps. You'll probably like this.
windowrule = suppressevent maximize, class:.*

# Fix some dragging issues with XWayland
windowrule = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0

# Work
windowrule = tile, initialClass:^(Microsoft-edge)$, initialTitle:^(Microsoft\ Teams)$
windowrule = tile, initialClass:^(Microsoft-edge)$, initialTitle:^(Outlook\ \(PWA\))$
windowrulev2 = workspace 3 silent, initialClass:^(Microsoft-edge)$
windowrulev2 = workspace 3 silent, initialClass:^(Microsoft-edge)$, initialTitle:^(Microsoft.Teams)$

# pavucontrol
windowrule = float, class:^(org.pulseaudio.pavucontrol)$, title:^(Volume Control)$, initialClass:^(org.pulseaudio.pavucontrol), initialTitle:^(Volume Control)$
windowrule = size 50 % 50 %, class:^(org.pulseaudio.pavucontrol)$, title:^(Volume Control)$, initialClass:^(org.pulseaudio.pavucontrol), initialTitle:^(Volume Control)$
windowrule = center, class:^(org.pulseaudio.pavucontrol)$, title:^(Volume Control)$, initialClass:^(org.pulseaudio.pavucontrol), initialTitle:^(Volume Control)$
windowrule = dimaround, class:^(org.pulseaudio.pavucontrol)$, title:^(Volume Control)$, initialClass:^(org.pulseaudio.pavucontrol), initialTitle:^(Volume Control)$
windowrule = stayfocused, class:^(org.pulseaudio.pavucontrol)$, title:^(Volume Control)$, initialClass:^(org.pulseaudio.pavucontrol), initialTitle:^(Volume Control)$


# kitty
# windowrule = opacity 0.98, initialClass:^(kitty)$, class:^(kitty)$


# # Code
windowrule = tile, initialClass:^(Code)$, initialTitle:^(Visual Studio Code)$
# windowrule = opacity 0.98, initialClass:^(Code)$, initialTitle:^(Visual Studio Code)$
# windowrulev2 = workspace 1, initialClass:^(Code)$, initialTitle:^(Visual Studio Code)$

# Gimp
# windowrulev2 = workspace 4 silent,class:^(gimp.*)$
windowrule = stayfocused, class:^(gimp-3.0)$, title:^(Quit GIMP)$, initialClass:^(gimp-3.0), initialTitle:^(Quit GIMP)$
windowrule = opacity 1, class:^(gimp-3.0)$, title:^(Quit GIMP)$, initialClass:^(gimp-3.0), initialTitle:^(Quit GIMP)$

# Tools
windowrulev2 = workspace special:social silent,class:^(Spotify)$
windowrulev2 = workspace special:social silent,class:^(Slack)$
windowrulev2 = workspace special:social silent,class:^(TelegramDesktop)$
windowrulev2 = workspace special:social silent,class:^(discord)$

# JetBrains (DataGrip, ...)
windowrule = stayfocused, class:^(jetbrains-.*)$,title:^(Data Sources and Drivers)$,initialClass:^(jetbrains-.*)$,initialTitle:^(Data Sources and Drivers)$
windowrule = stayfocused, class:^(jetbrains-.*)$,title:^(Confirm Exit)$,initialClass:^(jetbrains-.*)$,initialTitle:^(Confirm Exit)$,
# windowrule = tile, class:^(jetbrains-datagrip), title:^(win\d+)$, initialClass:^(jetbrains-datagrip), initialTitle:^(win\d+)$
# windowrule = stayfocused, class:^(jetbrains-datagrip), title:^(win\d+)$, initialClass:^(jetbrains-datagrip), initialTitle:^(win\d+)$

# On Screen Overlay
windowrule = float, initialClass:^(screenkey)$, initialTitle:^(screenkey)$
windowrule = opacity 1, initialClass:^(screenkey)$, initialTitle:^(screenkey)$
# windowrule = nofocus, initialClass:^(screenkey)$, initialTitle:^(screenkey)$
# windowrule = move onscreen 25% 80%, initialClass:^(screenkey)$, initialTitle:^(screenkey)$
# windowrule = size 50% 5%, initialClass:^(screenkey)$, initialTitle:^(screenkey)$
